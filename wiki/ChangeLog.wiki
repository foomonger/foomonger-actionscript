#labels Featured
#Describes major changes for each download version.
==v1.1.0==
  * Created AS3 version of !LoadWatcher created and tweaked a bit.  Tweaks reflected in AS2 version.

==v1.0.0==
  * Everything up on Google Code now.
  * Later.exec() is deprecated to distinguish "grouping" calls.  Now use `call()`, `gcall()`, `set()`, and `gset()`.